line-length = 120
target-version = "py311"

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    "E501",
    # FastAPI intentionally uses Depends/File/Cookie calls in endpoint defaults.
    "B008",
]
